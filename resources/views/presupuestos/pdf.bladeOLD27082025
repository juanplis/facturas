

<!DOCTYPE html>
<html lang="es">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Aptos+Narrow:wght@400&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Aptos Narrow', sans-serif; 
            font-size: 11px; 
            margin: 10px; 
        }
        .header, .header2 { 
            margin-bottom: 0; 
        }
        .banner {
            width: 120%; 
            height: auto; 
            display: block; 
            margin-left: -10%; 
            margin-right: -25%; 
            margin-top: -50px; 
            object-fit: cover; 
        }
        .table { 
            width: 100%; 
            border-collapse: collapse; 
            margin: 0; 
        }
        .table th, .table td { 
            border: 1px solid #000; 
            padding: 8px; 
        }
        .text-right { 
            text-align: right; 
        }
        .mb-4 { 
            margin-bottom: 1rem; 
        }
        .espacio { 
            height: 20px; /* Ajusta la altura del espacio entre filas */
        }
        .producto {
            margin-bottom: 5px; 
        }
        .producto strong {
            display: inline-block;
            width: 150px; 
        }
        @media print {
            .page-break {
                page-break-before: always;
            }
            .page-number {
                position: fixed;
                bottom: 10px;
                right: 20px;
                font-size: 10px;
            }
        }
    </style>
</head>
<body>

@if($presupuesto->empresa_id == 1)
    <img src="data:image/png;base64,{{ base64_encode(file_get_contents(public_path('storage/images/baner_steel.png'))) }}" alt="Banner" class="banner">
@else
    <img src="data:image/png;base64,{{ base64_encode(file_get_contents(public_path('storage/images/banner_tu_cocina.png'))) }}" alt="Banner" class="banner">
@endif

<div class="header">
    <table style="border-collapse: collapse;" border="0" width="500"> 
        <tr class="espacio">
            <th></th>
            <td>{{$descripcion}}</td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right;width:50px">
                <strong>PRESUPUESTO:</strong> COT{{ str_pad($presupuesto->id, 5, '0', STR_PAD_LEFT) }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right">
                <strong>CODIGO CLIENTE:</strong> {{ $presupuesto->cliente->id }}
            </td>
        </tr>
    </table>
</div>

<div class="header">
    <table style="border-collapse: collapse;" border="1" width="500"> 
        <tr class="espacio">
            <td style="width:50px">
                <strong>CLIENTE:</strong> {{ $presupuesto->cliente->nombre}}
            </td>
            <td style="width:50px">
                <strong>RIF:</strong> {{ $presupuesto->cliente->rif }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px">
                <strong>DIRECCIÓN:</strong> {{ $presupuesto->cliente->direccion }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CONTACTO:</strong> {{ $presupuesto->contactos->nombre }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CORREO:</strong> {{ $presupuesto->contactos->correo }}
            </td>
            <td style="text-align: right;">
                <strong>TELÉFONO:</strong> {{ $presupuesto->contactos->telefono }}
            </td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>FECHA:</strong> {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y')}}
            </td>
            <td style="text-align: right;">
                <strong>VALIDEZ:</strong> {{ $presupuesto->validez }}
            </td>
        </tr>
    </table>
</div> 

<table class="table mb-4" border="1">
    <thead>
        <tr class="espacio">
            <th>ITEM</th>
            <th>COD</th>
            <th>DESCRIPCIÓN</th>
            <th>CANT</th>
            <th>PRECIO UNITARIO</th>
            <th>PRECIO TOTAL</th>
        </tr>
    </thead>
    <tbody>
        @foreach($presupuesto->items as $item)
        <tr class="espacio">
            <td>{{ $item->id }}</td>
            <td>{{ $item->codigo }}</td>
            <td>{{ $item->descripcion }}</td>
            <td>{{ $item->cantidad }}</td>
            <td class="text-right">${{ number_format($item->precio_unitario, 2) }}</td>
            <td class="text-right">${{ number_format($item->precio_total, 2) }}</td>
        </tr>
        @endforeach
    </tbody>
</table>

@if($presupuesto->items->count() > 10) <!-- Cambia 10 por el número que consideres para un salto de página -->
    <div class="page-break"></div>
@endif

<div class="totals">
    <p class="text-right">SUB-TOTAL USD: ${{ number_format($presupuesto->subtotal - $presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">IVA: 16% ${{ number_format($presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">Descuento: % {{ $presupuesto->descuento }}</p>
    <p class="text-right">TOTAL USD: ${{ number_format($presupuesto->total, 2) }}</p>
</div>

<div class="page-number">
    Página {PAGE_NUM} de {PAGE_COUNT}
</div>

</body>
</html>


<!DOCTYPE html>
<html lang="es">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Aptos+Narrow:wght@400&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Aptos Narrow', sans-serif; 
            font-size: 11px; 
            margin: 10px; 
        }
        .header, .header2 { 
            margin-bottom: 0; 
        }
        .banner {
            width: 120%; 
            height: auto; 
            display: block; 
            margin-left: -10%; 
            margin-right: -25%; 
            margin-top: -50px; 
            object-fit: cover; 
        }
        .table { 
            width: 100%; 
            border-collapse: collapse; 
            margin: 0; 
        }
        .table th, .table td { 
            border: 1px solid #000; 
            padding: 8px; 
        }
        .text-right { 
            text-align: right; 
        }
        .mb-4 { 
            margin-bottom: 1rem; 
        }
        .espacio { 
            height: 20px; /* Ajusta la altura del espacio entre filas */
        }
        .producto {
            margin-bottom: 5px; 
        }
        .producto strong {
            display: inline-block;
            width: 150px; 
        }
        @media print {
            .page-break {
                page-break-before: always;
            }
        }
    </style>
</head>
<body>

@if($presupuesto->empresa_id == 1)
    <img src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/baner_steel.png'))); ?>" alt="Banner" class="banner">
@else
    <img src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/banner_tu_cocina.png'))); ?>" alt="Banner" class="banner">
@endif

<div class="header">
    <table style="border-collapse: collapse;" border="0" width="500"> 
        <tr class="espacio">
            <th></th>
            <td>{{$descripcion}}</td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right;width:50px">
                <strong>PRESUPUESTO:</strong> COT{{ str_pad($presupuesto->id, 5, '0', STR_PAD_LEFT) }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right">
                <strong>CODIGO CLIENTE:</strong> {{ $presupuesto->cliente->id }}
            </td>
        </tr>
    </table>
</div>

<div class="header">
    <table style="border-collapse: collapse;" border="1" width="500"> 
        <tr class="espacio">
            <td style="width:50px">
                <strong>CLIENTE:</strong> {{ $presupuesto->cliente->nombre}}
            </td>
            <td style="width:50px">
                <strong>RIF:</strong> {{ $presupuesto->cliente->rif }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px">
                <strong>DIRECCIÓN:</strong> {{ $presupuesto->cliente->direccion }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CONTACTO:</strong> {{ $presupuesto->contactos->nombre }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CORREO:</strong> {{ $presupuesto->contactos->correo }}
            </td>
            <td style="text-align: right;">
                <strong>TELÉFONO:</strong> {{ $presupuesto->contactos->telefono }}
            </td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>FECHA:</strong> {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y')}}
            </td>
            <td style="text-align: right;">
                <strong>VALIDEZ:</strong> {{ $presupuesto->validez }}
            </td>
        </tr>
    </table>
</div> 

<table class="table mb-4" border="1">
    <thead>
        <tr class="espacio">
            <th>ITEM</th>
            <th>COD</th>
            <th>DESCRIPCIÓN</th>
            <th>CANT</th>
            <th>PRECIO UNITARIO</th>
            <th>PRECIO TOTAL</th>
        </tr>
    </thead>
    <tbody>
        @foreach($presupuesto->items as $item)
        <tr class="espacio">
            <td>{{ $item->id }}</td>
            <td>{{ $item->codigo }}</td>
            <td>{{ $item->descripcion }}</td>
            <td>{{ $item->cantidad }}</td>
            <td class="text-right">${{ number_format($item->precio_unitario, 2) }}</td>
            <td class="text-right">${{ number_format($item->precio_total, 2) }}</td>
        </tr>
        @endforeach
    </tbody>
</table>

@if($presupuesto->items->count() > 10) <!-- Cambia 10 por el número que consideres para un salto de página -->
    <div class="page-break"></div>
@endif

<div class="totals">
    <p class="text-right">SUB-TOTAL USD: ${{ number_format($presupuesto->subtotal - $presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">IVA: 16% ${{ number_format($presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">Descuento: % {{ $presupuesto->descuento }}</p>
    <p class="text-right">TOTAL USD: ${{ number_format($presupuesto->total, 2) }}</p>
</div>

</body>
</html>

<!DOCTYPE html>
<html lang="es">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Aptos+Narrow:wght@400&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Aptos Narrow', sans-serif; 
            font-size: 11px; 
            margin: 10px; 
        }
        .header, .header2 { 
            margin-bottom: 0; 
        }
        .banner {
            width: 120%; 
            height: auto; 
            display: block; 
            margin-left: -10%; 
            margin-right: -25%; 
            margin-top: -50px; 
            object-fit: cover; 
        }
        .table { 
            width: 100%; 
            border-collapse: collapse; 
            margin: 0; 
        }
        .table th, .table td { 
            border: 1px solid #000; 
            padding: 8px; 
        }
        .text-right { 
            text-align: right; 
        }
        .mb-4 { 
            margin-bottom: 1rem; 
        }
        .espacio { 
            height: 20px; /* Ajusta la altura del espacio entre filas */
        }
        .producto {
            margin-bottom: 5px; 
        }
        .producto strong {
            display: inline-block;
            width: 150px; 
        }
    </style>
</head>
<body>

@if($presupuesto->empresa_id == 1)
    <img src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/baner_steel.png'))); ?>" alt="Banner" class="banner">
@else
    <img src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/banner_tu_cocina.png'))); ?>" alt="Banner" class="banner">
@endif

<div class="header">
    <table style="border-collapse: collapse;" border="0" width="500"> 
        <tr class="espacio">
            <th></th>
            <td>{{$descripcion}}</td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right;width:50px">
                <strong>PRESUPUESTO:</strong> COT{{ str_pad($presupuesto->id, 5, '0', STR_PAD_LEFT) }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px"></td>
            <td style="text-align: right">
                <strong>CODIGO CLIENTE:</strong> {{ $presupuesto->cliente->id }}
            </td>
        </tr>
    </table>
</div>

<div class="header">
    <table style="border-collapse: collapse;" border="1" width="500"> 
        <tr class="espacio">
            <td style="width:50px">
                <strong>CLIENTE:</strong> {{ $presupuesto->cliente->nombre}}
            </td>
            <td style="width:50px">
                <strong>RIF:</strong> {{ $presupuesto->cliente->rif }}
            </td>
        </tr>
        <tr class="espacio">
            <td style="width:50px">
                <strong>DIRECCIÓN:</strong> {{ $presupuesto->cliente->direccion }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CONTACTO:</strong> {{ $presupuesto->contactos->nombre }}
            </td>
            <td></td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>CORREO:</strong> {{ $presupuesto->contactos->correo }}
            </td>
            <td style="text-align: right;">
                <strong>TELÉFONO:</strong> {{ $presupuesto->contactos->telefono }}
            </td>
        </tr>
        <tr class="espacio">
            <td>
                <strong>FECHA:</strong> {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y')}}
            </td>
            <td style="text-align: right;">
                <strong>VALIDEZ:</strong> {{ $presupuesto->validez }}
            </td>
        </tr>
    </table>
</div> 

<table class="table mb-4" border="1">
    <thead>
        <tr class="espacio">
            <th>ITEM</th>
            <th>COD</th>
            <th>DESCRIPCIÓN</th>
            <th>CANT</th>
            <th>PRECIO UNITARIO</th>
            <th>PRECIO TOTAL</th>
        </tr>
    </thead>
    <tbody>
        @foreach($presupuesto->items as $item)
        <tr class="espacio">
            <td>{{ $item->id }}</td>
            <td>{{ $item->codigo }}</td>
            <td>{{ $item->descripcion }}</td>
            <td>{{ $item->cantidad }}</td>
            <td class="text-right">${{ number_format($item->precio_unitario, 2) }}</td>
            <td class="text-right">${{ number_format($item->precio_total, 2) }}</td>
        </tr>
        @endforeach
    </tbody>
</table>

<div class="totals">
    <p class="text-right">SUB-TOTAL USD: ${{ number_format($presupuesto->subtotal - $presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">IVA: 16% ${{ number_format($presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
    <p class="text-right">Descuento: % {{ $presupuesto->descuento }}</p>
    <p class="text-right">TOTAL USD: ${{ number_format($presupuesto->total, 2) }}</p>
</div>

</body>
</html>

<!DOCTYPE html>
<html>
<head>
  <link href="https://fonts.googleapis.com/css2?family=Aptos+Narrow:wght@400&display=swap" rel="stylesheet">
    <style>
   /*   @page { margin: 0px; }
		body { margin: 0px; }
*/
            .header2 { margin-bottom: 0px; }
        .table { width: 100%; border-collapse: collapse;}
        .table th, .table td { border: 1px solid #000; padding: 8px; }
        .text-right { text-align: right; }
        .mb-4 { margin-bottom: 1rem; }
        .banner {
         width: 100%;
            max-height: 150px;
            object-fit: cover;
            margin-bottom: 20px;
  
}
      .espacio{
					height:-200px;
				}
      .header { margin-bottom: 0px;  margin-top: -50px; }
        .table { width: 100%; border-collapse: collapse;  margin: 0px;     }
        .table th, .table td { border: 1px solid #000; padding: 8px;    }
        .text-right { text-align: right; }
        .mb-4 { margin-bottom: 1rem; }
        .banner {
            width: 120%; /* Ajusta el ancho del banner */
            height: auto; /* Mantiene la proporción de la imagen */
            display: block; /* Asegura que el banner se comporte como un bloque */
            margin-left: -10%; /* Compensa el exceso de ancho para centrarlo */
            margin-right: -25%; /* Compensa el exceso de ancho para centrarlo */
            margin-top: -50px; /* Ajusta este valor para la separación superior */
            object-fit: cover; /* Mantiene la proporción del banner */
        }
        body { 
            font-family: 'Aptos Narrow', sans-serif; 
            font-size: 11px; 
            margin:10px; 
        }
        .producto {
            margin-bottom: 5px; /* Espacio entre productos */
        }
        .producto strong {
            display: inline-block;
            width: 150px; /* Ancho fijo para etiquetas */
        }
    </style>
    <!--style>
        .header { margin-bottom: 20px; }
        .table { width: 100%; border-collapse: collapse; }
        .table th, .table td { border: 1px solid #000; padding: 8px; }
        .text-right { text-align: right; }
        .mb-4 { margin-bottom: 1rem; }
        .banner {
            width: 100%;
            max-height: 150px;
            object-fit: cover;
            margin-bottom: 20px;
        }
      
    </style-->
</head>
<body>
    <!-- SOLUCIÓN CON BASE64 -->


    @if($presupuesto->empresa_id == 1) {{-- ¡Ahora es un objeto! --}}
 <img  src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/baner_steel.png'))); ?>"
         alt="Banner"
         class="banner">
    </a>

    @else
 <img  src="data:image/png;base64,<?php echo base64_encode(file_get_contents(public_path('storage/images/banner_tu_cocina.png'))); ?>"
         alt="Banner"
         class="banner">
    @endif
  <div class="header">
<table style="border-collapse: collapse;" border="0"; width="500"> 
            <tr  class="espacio">
                <th></th>
                <td>{{$descripcion}}</td>

            </tr>

            <tr  class="espacio">
               <!-- th>{{$direccion}}</th-->
                <td style="width:50px"> <p class="text-left"></p>
 				</td>
              <td style="text-align: right;width:50px">
                <p class="text-left"  ><strong>PRESUPUESTO:</strong>COT{{ str_pad($presupuesto->id, 5, '0', STR_PAD_LEFT) }}</p>
                </td>
            </tr>
  			<tr  class="espacio">
               <!--th>{{$direccion}}</th-->
                <td  style="width:50px"> <p class="text-left"></p>
 				</td>
              <td style="text-align: right">
                  <p class="text-left"  ><strong>CODIGO CLIENTE:</strong>{{ $presupuesto->cliente->id }}</p>
                </td>
            </tr>
    </table>
  </div>
       <?php

/*
    <div class="header">
        <p></p>
        <!--p class="text-right">PRESUPUESTO: COT{{ str_pad($presupuesto->id, 5, '0', STR_PAD_LEFT) }}</p>
        <p class="text-right"> CODIGO CLIENTE: {{ $presupuesto->cliente->id }}</p-->
        
      	<!--p>CLIENTE: {{ $presupuesto->cliente->nombre }}</p>
        <p class="text-right">N° RIF: {{ $presupuesto->cliente->rif }}</p>


        <p>DIRECCIÓN: {{ $presupuesto->cliente->direccion }}</p>
        <p>CONTACTO: {{ $presupuesto->contactos->nombre }}</p>
        <p  class="text-right">TELEFONO: {{ $presupuesto->contactos->telefono }}</p>
        <p>CORREO: {{ $presupuesto->contactos->correo }}</p>

        <p>FECHA: {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y') }}</p>
        <p class="text-right"> VALIDEZ: {{ $presupuesto->validez }}</p-->

      
        <p><strong>Cliente:</strong> {{ $presupuesto->cliente->nombre}}</p>
        <p class="text-right" ><strong>RIF:</strong> {{ $presupuesto->cliente->rif }}</p>
        <p><strong>Dirección:</strong> {{ $presupuesto->cliente->direccion }}</p>
        <p><strong>Contacto:</strong> {{ $presupuesto->contactos->nombre }}</p>
        <p  class="text-right"><strong>Telefono:</strong> {{ $presupuesto->contactos->telefono }}</p>
        <p><strong>Correo:</strong> {{ $presupuesto->contactos->correo }}</p>

        <p><strong>Fecha:</strong> {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y')}}</p>
        <p class="text-right"> VALIDEZ: {{ $presupuesto->validez }}</p>

    </div>
    */
?>
  
  <div class="header">
    

<table style="border-collapse: collapse;" border="1"; width="500"> 
            <tr  class="espacio">
                <td style="width:50px"> <p  class="text-left;"><strong>CLIENTE:</strong> {{ $presupuesto->cliente->nombre}}</p>
 				</td>
              <td style="width:50px">
                  <p style="text-align: right;"  ><strong>RIF:</strong> {{ $presupuesto->cliente->rif }}</p>
                </td>
            </tr>
    		<tr  class="espacio">
                 <td style="width:50px"> <p class="text-left;"><strong>DIRECCÍON:</strong> {{ $presupuesto->cliente->direccion }}</p>
 				</td>
              <td >
                  <p class="text-left" ></p>
                </td>
            </tr>
    		<tr  class="espacio" height:50px;>
                <td> <p class="text-left;"><strong>CONTACTO:</strong> {{ $presupuesto->contactos->nombre }}</p>
 				</td>
              <td style="width:50px;">
                  <p class="text-left" ></p>
                </td>
            </tr>
  			<tr  class="espacio">
                <td> <p class="text-left;width:50px;"><strong>CORREO:</strong> {{ $presupuesto->contactos->correo }}</p>
 				</td>
              <td style="text-align: right;width:50px;">
                  <p class="text-left"><strong>TELÉFONO:</strong> {{ $presupuesto->contactos->telefono }}</p>
                </td>
            </tr>
 			 <tr  class="espacio">
                <td> <p class="text-left;width:50px;"><strong>FECHA:</strong> {{ \Carbon\Carbon::parse($presupuesto->fecha)->format('d/m/Y')}}</p>
 				</td>
			 <td style="width:50px">
                  <p style="text-align: right;"  ><strong>VALIDEZ:</strong>  {{ $presupuesto->validez }}</p>
                </td>
            </tr>
    </table>
   </div> 
    <table class="table mb-4" border="1">
        <thead>
            <tr  class="espacio">
                <th>ITEM</th>
                <th>COD</th>
                <th>DESCRIPCIÓN</th>
                <th>CANT</th>
                <th>PRECIO UNITARIO</th>
                <th>PRECIO TOTAL</th>
            </tr>
        </thead>
        <tbody>
          @foreach($presupuesto->items as $item)
            <tr  class="espacio">
                <td>{{ $item->id }}</td>
                <td>{{ $item->codigo }}</td>
                <td>{{ $item->descripcion }}</td>
                <td>{{ $item->cantidad }}</td>


    @if($presupuesto->empresa_id == 1) {{-- ¡Ahora es un objeto! --}}
                <td class="text-right">${{ number_format($item->precio_unitario, 2) }}</td>
                <td class="text-right">${{ number_format($item->precio_total, 2) }}</td>
    @else
                <td class="text-right">${{ number_format($item->precio_unitario / 0.7 , 2) }}</td>
                <td class="text-right">${{ number_format($item->precio_total / 0.7 , 2) }}</td>

    @endif
            </tr>
            @endforeach
         
          

          
          
        </tbody>
    </table>
  
        </br>
        </br>
        </br>
        </br>
        </br>
        </br>
        </br>
        </br>
        </br>

    <div class="totals">


          @if($presupuesto->empresa_id == 1) {{-- ¡Ahora es un objeto! --}}
        <p class="text-right">SUB-TOTAL USD: ${{ number_format($presupuesto->subtotal - $presupuesto->subtotal  * $presupuesto->iva / 100, 2) }}</p>
        <p class="text-right">IVA: 16% ${{ number_format($presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
        <p class="text-right">Descuento: % {{ $presupuesto->descuento }}</p>
        <p class="text-right">TOTAL USD: ${{ number_format($presupuesto->total, 2) }}</p>
          @else
        <p class="text-right">SUB-TOTAL USD: ${{ number_format($presupuesto->subtotal - $presupuesto->subtotal  * $presupuesto->iva / 100, 2 ) }}</p>
        <p class="text-right">IVA: 16% ${{ number_format($presupuesto->subtotal * $presupuesto->iva / 100, 2) }}</p>
        <p class="text-right">Descuento: % {{ $presupuesto->descuento }}</p>
        <p class="text-right">TOTAL USD: ${{ number_format($presupuesto->total, 2) }}</p>
         @endif

    </div>
</body>
</html>
